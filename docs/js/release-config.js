/* ============================================================
   release-config.js
   Edit the dates below whenever you want to update the
   "next chapter releases in" countdown shown on book.html.

   Format: "YYYY-MM-DD" (24-hour, treated as midnight local time)
   Set a book's value to null if there's no scheduled release yet.
   ============================================================ */

window.ProjectRoseReleaseConfig = {
  1: null,               // Book One — fully released, no countdown needed
  2: "2026-08-15",       // Book Two — next chapter (5) drops on this date
  3: null,               // Book Three — not yet scheduled
};
